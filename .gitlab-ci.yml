article:
  stage: deploy
  image: rocker/verse:3.5.1
  script:
    - Rscript -e "devtools::install_github('hadley/emo')"
    - Rscript -e "rmarkdown::render('supports/01_preparer_travail_equipe/01_main.Rmd')"
    - Rscript -e "rmarkdown::render('supports/02_functions/02_main.Rmd')"
  artifacts:
    paths:
      - "supports/01_preparer_travail_equipe"
      - "supports/02_functions"
    when: always
